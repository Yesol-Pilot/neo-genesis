# Sora Cloud Runtime Dependencies
# 로컬 전용 ML 패키지(torch, jax, flax 등) 제외

# ── Core Framework ──
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.6
starlette>=0.27.0
httpx>=0.24.0

# ── Telegram Bot ──
python-telegram-bot>=20.0

# ── Gemini AI ──
google-genai>=1.0.0

# ── RAG (ChromaDB + Embeddings) ──
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── LangChain (기존 코드 호환) ──
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.1

# ── Scheduler ──
apscheduler>=3.10.0

# ── Utils ──
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0
pydantic>=2.0.0
tenacity>=8.2.0
rich>=13.0.0
beautifulsoup4>=4.12.0

# ── Data ──
pandas>=2.0.0
numpy>=1.24.0

# ── Supabase ──
supabase>=2.0.0

# ── Search ──
duckduckgo-search>=4.0.0

# ── Web ──
schedule>=1.2.0
Pillow>=10.0.0

# ── WebSocket (PC Agent Hub) ──
websockets>=12.0

# ── Session / Auth ──
itsdangerous>=2.0.0

# ── Redis (Message Bus) ──
redis[hiredis]>=5.0.0

# ── Anthropic Claude API ──
anthropic>=0.40.0

# ── HTTP Client (Telegram webhook) ──
httpx>=0.24.0
